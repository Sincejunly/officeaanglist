include /etc/nginx/includes/http-common.conf;
server {
  listen 0.0.0.0:80;
  listen [::]:80 default_server;
  real_ip_recursive on;
  error_log /var/log/nginx/error.log;
  location ^~ /viewer {
    
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
		proxy_set_header Host $http_host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header Range $http_range;
		proxy_set_header If-Range $http_if_range;
		proxy_redirect off;
    alias /var/www/app/viewer/auth;
    index index.html;
    try_files $uri $uri/ /index.html;
  }

  location /callback/ {
    root /var/www/app;
    index dataTo.php;
    try_files $uri $uri/ /dataTo.php?query_string;
  }

  location ~ \.php$ {
	root /var/www/app;
    include fastcgi_params;
    fastcgi_pass unix:/run/php/php7.4-fpm.sock;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
  }

  # location /office {
  #   rewrite ^/office(.*) $1 break;
  #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #   proxy_set_header X-Forwarded-Proto $scheme;
  #   proxy_set_header Host $http_host;
  #   proxy_set_header X-Real-IP $remote_addr;
  #   proxy_set_header Range $http_range;
  #   proxy_set_header If-Range $http_if_range;
  #   proxy_redirect off;

  #   proxy_pass http://127.0.0.1:3000;

  # }

   location /aria2/ {
    proxy_pass http://127.0.0.1:6800/;
    proxy_redirect off;
    proxy_set_header        X-Real-IP       $remote_addr;
    proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Host $host;
    
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }

  #  location /AriaNg {
  #   alias /var/www/app/AriaNg;
  #   index index.html;
  #   try_files $uri $uri/ /index.html?query_string;
  # }

  location /AList/ {
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Range $http_range;
    proxy_set_header If-Range $http_if_range;
    proxy_redirect off;

    proxy_pass http://127.0.0.1:5244/AList/;
    # the max size of file to upload
    client_max_body_size 0;
  }

  location /qbit {
    proxy_http_version 1.1;
    proxy_set_header X-Forwarded-Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    http2_push_preload on;


    proxy_pass http://172.17.0.1:6901;
  }
  location ~ ^/(orc){
    proxy_pass http://127.0.0.1:5001;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
       
  }
  location ~ ^/(query|delete|update|check|register|count|login|logout|generate_code|orc|AListPath|savePath|ChangeUser|checkUser) {
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header Range $http_range;
    proxy_set_header If-Range $http_if_range;
    proxy_redirect off;
    proxy_pass   http://127.0.0.1:5000;
  }
		
		location /user {
			proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
			proxy_set_header X-Forwarded-Proto $scheme;
			proxy_set_header Host $http_host;
			proxy_set_header X-Real-IP $remote_addr;
			proxy_set_header Range $http_range;
			proxy_set_header If-Range $http_if_range;
			proxy_redirect off;
			alias /var/www/app/viewer/auth;
			index user.html;
			try_files $uri $uri/ /user.html;
		}

  include /etc/nginx/includes/ds-*.conf;
}
